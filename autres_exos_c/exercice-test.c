#include <stdio.h>

int main() {
//calcul somme nombres pairs jusqu'à nombre donné
    int nbN, i, somme;

    printf("Entrez un nombre entier positif\n");
    scanf("%d", nbN);

        if (nbN < 0) {
            printf("Votre nombre est négatif");
        } else if (nbN > 0) {
            printf("Votre nombre est positif");
        } else {
            printf("Votre nombre est %d et il est positif", nbN);
        }

        for () {
            
        }
        







    return 0;
}